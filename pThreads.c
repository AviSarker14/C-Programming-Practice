#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <pthreads.h>
void* helpPi(void*arg)) {
    int n, numprocs, i;
    double PI25DT = 3.141592653589793238462643;
    double pi,sum, partial_sum, x;
    thread_numprocs = *(int *)numprocs
    


}

int main(int argc, char *argv[]) {
    int NUM_THREADS;
    if(argc != 2) {
        NUM_THREADS = 3
    }
    else {
        NUM_THREADS = atoi(argv[1]);
    }
    pthread_t *tid = (pthread_t*) malloc(sizeof(pthread_t)*NUM_THREADS)
    int error;
    int n = 0;

    
    
}
